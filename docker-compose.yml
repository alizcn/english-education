services:
  web:
    build: .
    container_name: english-education
    restart: unless-stopped
    ports:
      - "8070:8000"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/db.sqlite3
    volumes:
      - ./data:/app/data
